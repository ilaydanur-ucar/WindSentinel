# ── RabbitMQ ──
RABBITMQ_USER=your_rabbitmq_username
RABBITMQ_PASS=your_rabbitmq_password_min_16_chars
RABBITMQ_PORT=5672
RABBITMQ_MGMT_PORT=15672

# ── RabbitMQ Connection ──
RABBITMQ_URL=amqp://${RABBITMQ_USER}:${RABBITMQ_PASS}@rabbitmq:5672

# ── PostgreSQL ──
POSTGRES_DB=windsentinel
POSTGRES_USER=your_db_username
POSTGRES_PASSWORD=your_db_password_min_16_chars
POSTGRES_PORT=5434

# ── Database Connection ──
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
DATABASE_URL_LOCAL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}

# ── API Gateway ──
JWT_SECRET=generate_with_openssl_rand_hex_32_or_node_crypto
API_PORT=8000

# ── Data Simulator ──
SIMULATOR_INTERVAL_SECONDS=600
SIMULATOR_BATCH_SIZE=10

# ── Data Consumer ──
CONSUMER_INPUT_QUEUE=sensor-data
CONSUMER_OUTPUT_QUEUE=measurement.raw
CONSUMER_EXCHANGE_NAME=wind.events
CONSUMER_ROUTING_KEY=measurement.raw
CONSUMER_PREFETCH_COUNT=1

# ── Generate strong values ──
# node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
# openssl rand -hex 32
